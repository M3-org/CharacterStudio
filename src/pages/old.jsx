import React from "react";

function App() {
  return null // (
            // <Background />
        // <Logo />
        //   <Scene manifest={manifest} sceneModel={sceneModel} initialTraits={initialTraits} templateInfo={templateInfo} />
        //   <div style = {{display:(hideUi ? "none" : "block")}}>
        //     <Fragment >
        //     <ChatButton />
        //   {/* <ARButton /> */}
        //   <UserMenu />
        //   {viewMode === ViewMode.CHAT && <ChatComponent />}
        //   {viewMode === ViewMode.APPEARANCE && <Editor manifest = {manifest} animationManager={animationManager} initialTraits={initialTraits} templateInfo={templateInfo} blinkManager={blinkManager} fetchNewModel={fetchNewModel}/>}
  // )
}

export default App;