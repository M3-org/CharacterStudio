import styled from 'styled-components';

const style = styled.div`
    {
        background: rgba(23, 22, 31, 0.35);
        border: 1px solid #38404E;
        backdrop-filter: blur(22.5px);
        border-radius: 5px;
        width : 54px;
        height : 54px;
        display : flex;
        justify-content : center;
        align-items : center;
        .buttonImg {
            display : inline-block;
            width : 70%;
            margin : auto;
        }
    }
`

export default style;